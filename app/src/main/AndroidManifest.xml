<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tugasintent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tugasintent.NoActionBar">

        <activity
            android:label="Dataform"
            android:name=".Dataform"
            android:parentActivityName=".Form"
            android:theme="@style/Theme.Tugasintent"></activity>

        <activity android:name=".Penjelasan" />

        <activity android:name=".Awal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>